import { createStyles, rem } from "@mantine/core";

const homeStyles = createStyles((theme) => ({
  pageContainer: {
    // height: "100vh",
    display: "flex",
    flexDirection: "column",
    height: '100%',
    backgroundColor: "#F3FFF2",
  },

  landingBg: {
    height: "100vh",
  },
}));
export { homeStyles };
