import { createStyles } from "@mantine/core";
const headerStyle = createStyles((theme) => ({
  header: {
    display: "flex",
    alignItems: "center",
    backgroundColor: "red",
    width:'100%'
  },
}));
export { headerStyle };
