import { createStyles, rem } from "@mantine/core";

const featuresStyles = createStyles((theme) => ({
  featuresContainer: {
    backgroundColor: "#C5DFA5",
    flexDirection: "column",
    padding: "150px 156px",
    gap: "100px",
    color: "#151810",
  },

  textGroup: {
    flexDirection: "column",
    gap: 30,
  },

  featuresWrapper: {
    flexDirection: "column",
    gap: "100px",
  },
}));
export { featuresStyles };
