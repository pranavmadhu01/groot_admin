import { createStyles, rem } from "@mantine/core";

const addFertilizerFormStyle = createStyles((theme) => ({
  contentWrapper: { gap: 15, flexDirection: "column" },
  inputWrapper: { gap: 15, flexDirection: "column" },
}));
export { addFertilizerFormStyle };
